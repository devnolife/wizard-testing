feat: framework-agnostic app discovery (Phase 7)

Generalize discover_app beyond Next.js. Detect the target framework from
package.json deps + scripts (Next.js, SvelteKit, Remix, Astro, Nuxt, Vite,
CRA) and map UI routes/API handlers per framework convention. Auto-select
the dev script (dev -> start). For client-rendered SPAs where routes can't
be derived from the file tree, return a note and let the agent crawl links
via the new list_links tool so coverage degrades gracefully.

- server/tools/codebase.ts: detectFramework() + framework-aware discoverApp()
  with per-framework route mappers and SPA fallback (["/"] + note).
- server/tools/browser.ts: listLinks() collects same-origin internal links.
- server/copilot/tools.ts: framework-aware discover_app message, new
  list_links tool, added to TOOL_NAMES.
- server/copilot/runner.ts: framework-detection step, passes detected dev
  script to ensureRunning, prompt mentions list_links for SPAs.
- test/codebase.test.ts: detection + multi-framework route + SPA fallback.
- README + .gitignore (fixtures test-results).

Verified: lint 0 errors, 57 tests green, build OK; live run against the
Next.js fixture reports "Detected nextjs: 3 pages, 3 API routes".

Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>
